{% extends "base.html" %}
{% block title %}报修二维码 · {{ hospital.name }}{% endblock %}
{% block content %}
<div class="text-center py-4">
    <h5><i class="fas fa-qrcode text-primary"></i> {{ hospital.name }} 报修二维码</h5>
    {% if department %}
    <p class="text-muted">{{ department }}</p>
    {% else %}
    <p class="text-muted">全院通用</p>
    {% endif %}
    <div class="d-inline-block p-4 bg-white rounded shadow-sm" style="border:2px solid #e2e8f0;">
        <img src="{{ url_for('scan.qr_image', hospital_id=hospital.id, department=department or '') }}" 
             alt="报修二维码" style="width:280px;height:280px;image-rendering:pixelated;">
    </div>
    <div class="mt-3">
        <p class="text-muted small">扫一扫上面的二维码，提交故障报修</p>
        <button class="btn btn-primary" onclick="window.print()"><i class="fas fa-print"></i> 打印二维码</button>
        <a href="{{ url_for('scan.scan_department', hospital_id=hospital.id) }}" class="btn btn-outline-secondary"><i class="fas fa-arrow-left"></i> 返回</a>
    </div>
    <div class="mt-4 alert alert-info small text-start d-inline-block" style="max-width:500px;">
        <strong><i class="fas fa-info-circle"></i> 使用方法</strong><br>
        ① 将此二维码打印后贴在科室显眼位置<br>
        ② 医护人员微信扫一扫 → 打开报修表单<br>
        ③ 填写姓名、电话、故障描述 → 提交<br>
        ④ 系统自动创建工单并推送通知工程师
    </div>
</div>
{% endblock %}
