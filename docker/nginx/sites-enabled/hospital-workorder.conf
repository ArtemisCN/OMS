server {
    server_name demolin.cn 118.89.69.177;

    client_max_body_size 30m;

    # ===== 上传目录（Docker 卷 /app/uploads，与 web 容器共享）=====
    # 优先级高于下面的 /static/ 通用 location（nginx 最长前缀匹配）
    location /static/uploads/wochat/ {
        alias /app/uploads/wochat/;
        expires 30d;
        add_header Cache-Control "public, immutable";
    }
    location /static/uploads/chat/ {
        alias /app/uploads/chat/;
        expires 30d;
        add_header Cache-Control "public, immutable";
    }
    location /static/uploads/hospitals/ {
        alias /app/uploads/uploads/hospitals/;
        expires 30d;
        add_header Cache-Control "public, immutable";
    }
    location /static/hospital_maps/ {
        alias /app/uploads/hospital_maps/;
        expires 30d;
        add_header Cache-Control "public, immutable";
    }
    location /static/dt_textures/ {
        alias /app/uploads/dt_textures/;
        expires 30d;
        add_header Cache-Control "public, immutable";
    }

    location /static/ {
        alias /var/www/static/;
        expires 30d;
        add_header Cache-Control "public, immutable";
    }

    location = /api/mobile/login {
        limit_req zone=login_limit burst=1 nodelay;
        proxy_pass http://127.0.0.1:5000;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
    }

    location /ws/ {
        proxy_pass http://127.0.0.1:5100/;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection "upgrade";
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_read_timeout 86400;
    }

    location / {
        proxy_pass http://127.0.0.1:5000;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
    }

    location /dl/ {
        root /var/www/html;
    }

    listen 443 ssl;
    ssl_certificate /etc/letsencrypt/live/demolin.cn/fullchain.pem;
    ssl_certificate_key /etc/letsencrypt/live/demolin.cn/privkey.pem;
    include /etc/letsencrypt/options-ssl-nginx.conf;
    ssl_dhparam /etc/letsencrypt/ssl-dhparams.pem;

    add_header X-Frame-Options "SAMEORIGIN" always;
    add_header X-Content-Type-Options "nosniff" always;
    add_header Strict-Transport-Security "max-age=31536000; includeSubDomains" always;
}

server {
    listen 80;
    server_name demolin.cn 118.89.69.177;

    location /dl/ {
        root /var/www/html;
    }

    location / {
        return 301 https://$host$request_uri;
    }
}
